name: PR Build

on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          GITHUB_ACTIONS: true
          VITE_BUILD_REF: pr#${{ github.event.pull_request.number }}
      - uses: actions/upload-artifact@v4
        with:
          name: pr-${{ github.event.pull_request.number }}-dist
          path: dist
